# api-types

Type TypeScript được sinh từ `openapi.yaml`. Dùng chung giữa `apps/web-app`, `apps/mobile-app`, `packages/sdk-js`.

Sinh code:
```bash
npx openapi-typescript ../../openapi.yaml -o src/index.ts
```

Chưa generate ở v0.
