[project]
name = "lora-coverage-sdk"
version = "0.0.0"
description = "Python SDK — placeholder, chưa implement."
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["lora_coverage_sdk"]
