# ── Python ─────────────────────────────────────
__pycache__/
*.py[cod]
*.so
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
venv/
.venv/
env/

# Testing / linting cache
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/

# ── Node ───────────────────────────────────────
node_modules/
.pnp
.pnp.js
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.parcel-cache
.next/
.nuxt/
.cache/
.turbo/

# ── Vite / React build output ──────────────────
**/dist/
**/.vite/

# ── Secrets (12-Factor F3) ─────────────────────
.env
.env.local
.env.*.local
*.env
!.env.example
!.env.template

# ── IDE ────────────────────────────────────────
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# ── Dữ liệu lớn (không commit) ─────────────────
# DEM tiles: kích thước ~25MB/file, lấy từ NASA SRTM, không cần lưu git
services/ml-service-hmap/data/dem/*.hgt
services/ml-service-hmap/data/dem/*.tif

# ML model artifacts: lưu trên S3/R2, key prefix có model_version
services/ml-service-hmap/models/*.onnx
services/ml-service-hmap/models/*.joblib
services/ml-service-hmap/models/*.pkl

# Stage 2 Predict-ML artifacts (LightGBM booster + meta) — local cache,
# canonical store là S3/R2 + ml.model_runs registry.
services/ml-service-predict/artifacts/

# Stage 2 evaluation report output (PNG + summary.txt) — sinh từ
# `python -m evaluation.generate_report`, có thể tái tạo bất kỳ lúc nào.
services/ml-service-predict/evaluation/reports/

# Tile cache (worker output)
services/tile-server/cache/

# ── Database files ─────────────────────────────
*.sqlite
*.sqlite3
*.db

# ── Logs ───────────────────────────────────────
*.log
logs/

# ── Legacy code (đã archive, không cần index) ──
# legacy/  ← uncomment nếu muốn loại khỏi git index sau này

# ── OS / misc ──────────────────────────────────
*.pid
*.tmp
*.bak

# ── Personal / local config (không commit) ─────────────────
.claude/
u-work/
core-logic/skills/
thumucluucacfilemdcu/
core-logic/main-logic/pics/