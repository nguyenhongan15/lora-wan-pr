# ── Python ─────────────────────────────────────
__pycache__/
*.py[cod]
*.so
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
venv/
.venv/
env/

# Testing / linting cache
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/
lora-data/

# ── Node ───────────────────────────────────────
node_modules/
.pnp
.pnp.js
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.parcel-cache
.next/
.nuxt/
.cache/
.turbo/

# ── Vite / React build output ──────────────────
**/dist/
**/.vite/

# ── Secrets (12-Factor F3) ─────────────────────
.env
.env.local
.env.*.local
*.env
!.env.example
!.env.template

# ── IDE ────────────────────────────────────────
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# ── Archive (không index trên remote) ─────────
archive/

# Tile cache (worker output)
services/tile-server/cache/

# ── Database files ─────────────────────────────
*.sqlite
*.sqlite3
*.db

# ── Logs ───────────────────────────────────────
*.log
logs/

# ── Legacy code (đã archive, không cần index) ──
# legacy/  ← uncomment nếu muốn loại khỏi git index sau này

# ── OS / misc ──────────────────────────────────
*.pid
*.tmp
*.bak

# ── Personal / local config (không commit) ─────────────────
.claude/
u-work/
core-logic/
.vscode/settings.json