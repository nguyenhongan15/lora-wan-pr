# ── Python ─────────────────────────────────────
__pycache__/
*.py[cod]
*.so
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
venv/
.venv/
env/

# Testing / linting cache
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/

# ── Node ───────────────────────────────────────
node_modules/
.pnp
.pnp.js
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.parcel-cache
.next/
.nuxt/
.cache/
.turbo/

# ── Vite / React build output ──────────────────
**/dist/
**/.vite/

# ── Secrets (12-Factor F3) ─────────────────────
.env
.env.local
.env.*.local
*.env
!.env.example
!.env.template

# ── IDE ────────────────────────────────────────
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# ── Dữ liệu lớn (không commit) ─────────────────
# Evaluation report PNG output cho archive/stage2-lightgbm — sinh từ
# `python -m evaluation.generate_report`, có thể tái tạo bất kỳ lúc nào.
# (Artifact LightGBM 3.3 MB được commit kèm code, xem archive/stage2-lightgbm/README.md.)
archive/stage2-lightgbm/evaluation/reports/

# Tile cache (worker output)
services/tile-server/cache/

# ── Database files ─────────────────────────────
*.sqlite
*.sqlite3
*.db

# ── Logs ───────────────────────────────────────
*.log
logs/

# ── Legacy code (đã archive, không cần index) ──
# legacy/  ← uncomment nếu muốn loại khỏi git index sau này

# ── OS / misc ──────────────────────────────────
*.pid
*.tmp
*.bak

# ── Personal / local config (không commit) ─────────────────
.claude/
u-work/
core-logic/skills/
thumucluucacfilemdcu/
core-logic/main-logic/pics/