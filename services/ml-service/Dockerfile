# SKELETON — chưa runnable. Khi build thật:
#   - Multi-stage giống api-service (builder + runtime slim-bookworm)
#   - Pre-download ONNX runtime + model artifacts từ R2
#   - Non-root user, tini PID 1, healthcheck
#   - Stage 3+: dùng base image có CUDA nếu cần GPU
FROM python:3.12.7-slim-bookworm
WORKDIR /app
LABEL org.opencontainers.image.description="ml-service skeleton — not yet runnable"
CMD ["echo", "ml-service skeleton: implement Stage 2+ trước khi build."]
